package main

import "fmt"

func main() {

	// Use Value semantics by default

	// Use pointer semantics for large data sets

	// Use Pointer semantics for Mutability

	// Consistency - once Pointer semantics have been used always use pointer semantics

	// Use Pointer semantics when Interfacing with other code e.g a library or system call
}