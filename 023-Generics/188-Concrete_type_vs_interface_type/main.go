package main

import "fmt"

func main() {
	//Concrete type = int, bool, string, float64, arrays, slices, maps, structs, etc...

	//Interface types are abstract, they represent behavior or type but not a specific set of values
}